apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: phpapp
  name: phpapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phpapp
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: phpapp
    spec:
      containers:
      - image: php:8.3.21-zts-alpine3.20
        name: phpapp
        resources: {}
status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: flask-service
  labels:
    app: flask-service
spec:
  type: NodePort
  ports:
  - nodePort: 30001
    port: 80
    targetPort: 5000
  selector:
    app: phpapp
